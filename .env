POSTGRES_USER=postgres
POSTGRES_PASSWORD=SuperSecretSchoolDbPass2026!
POSTGRES_DB=school_management

DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=SuperSecretSchoolDbPass2026!
DB_NAME=school_management

PORT=4000
JWT_SECRET=SuperSecureAustrianSchoolJwtSecretKey998877!

# For browser-only access on the same machine: keep localhost
# For iPad/mobile/classroom access: replace with your server's LAN IP
# Run `ipconfig` (Windows) or `ip a` (Linux) to find it — e.g. 192.168.1.100
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_WS_URL=http://localhost:4000

# Web Push VAPID Configuration Keys (Auto-generated on startup if blank)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@example.com
